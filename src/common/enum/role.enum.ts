export enum FetureCode {
  admin = 'adm',
  marketing = 'mkt',
  content = 'cte',
  default = 'def',
}
export enum Roles {
  admin = 'admin',
  marketing = 'marketing',
  content = 'content',
  default = 'default',
  ucp_admin = 'adm',
  ucp_user = 'use',
}
