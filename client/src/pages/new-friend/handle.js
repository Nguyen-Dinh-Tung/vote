import { ApiBase } from '../../api/api.base';

export class Handle {
  static addFriend(data, setReRenderUnknowPeople) {
    const urlAddNotifyApp = '/notify-app';
    // ApiBase.post(urlAddFriend, data)
    //   .then((res) => {
    //     if (res.status === 201) {
    //       console.log(res);
    //       setReRenderUnknowPeople(Date.now());
    //     }
    //   })
    //   .catch((e) => {
    //     if (e) console.log(e);
    //   });
  }
}
